import 'package:flutter/material.dart';

class ItemMovie extends StatelessWidget {
  ItemMovie({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      margin: EdgeInsets.all(10),
      width: 250,
      height: 100,
      color: Colors.blue,
      // TODO: crear el widget que sera utilizado para mostrar pelicula
    );
  }
}
